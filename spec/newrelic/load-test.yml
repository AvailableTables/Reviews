config:
  target: 'http://localhost:3020'
  phases:
    - duration: 1800
      arrivalRate: 200
  processor: './randomID.js'
scenarios:
  - flow:
    - function: "randomID"
    - get:
        url: "/restaurants/{{ id }}"
    - get:
        url: "/API/Reviews/reviews/all?{{ id }}"

