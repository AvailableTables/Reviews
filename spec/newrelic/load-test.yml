config:
  target: 'http://localhost:3020'
  phases:
    - duration: 60
      arrivalRate: 50
  processor: './randomID.js'
scenarios:
  - flow:
    - loop:
        - function: "randomID"
        - get:
            url: "/API/Reviews/reviews/all?{{ id }}"
      count: 10
  
