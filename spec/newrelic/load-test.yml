config:
  target: 'http://localhost:3020'
  phases:
    - duration: 10
      arrivalRate: 100
  processor: './randomID.js'
scenarios:
  - flow:
    - loop:
        - function: "randomID"
        - get:
            url: "/restaurants/{{ id }}"
      count: 2
  
